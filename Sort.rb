class Sort
  def bubble_sort
    rightmost = list.length - 1
      until rightmost < 1 do
        ordered_flag = true
        left = 0
        until left >= rightmost do
            
        end
        rightmost -= 1
      end
  end
end